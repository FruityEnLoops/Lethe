{
  "name": "lethe",
  "version": "1.0.0",
  "description": "Twitter bot to trigger a webhook when a new tweet is sent by a user",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "FruityEnLoops",
  "license": "ISC",
  "dependencies": {
    "node-fetch": "^2.6.1",
    "twit": "^2.2.11"
  }
}
